<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/widget_margin">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#bbDEDFDE"
        android:orientation="vertical">
        <LinearLayout
            android:id="@+id/widget_layout_main"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimaryDark"
            android:orientation="horizontal"
            android:paddingLeft="@dimen/widget_header_padding"
            android:paddingRight="@dimen/widget_header_padding"
            android:elevation="@dimen/widget_header_elevation">
            <ImageView
                android:id="@+id/widget_img_launcher"
                android:layout_width="@dimen/widget_icon_width"
                android:layout_height="@dimen/widget_icon_width"
                android:layout_gravity="center_vertical"
                android:src="@mipmap/ic_launcher"/>
            <TextView
                android:id="@+id/widget_header"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Widget Header"
                android:layout_gravity="center_vertical"
                android:textColor="@color/menu_nav_contrast"
                android:textAppearance="?android:attr/textAppearanceMedium"
                android:textStyle="bold"
                android:paddingStart="@dimen/widget_text_padding"
                android:paddingLeft="@dimen/widget_text_padding"
                android:paddingEnd="@dimen/widget_text_padding"
                android:paddingRight="@dimen/widget_text_padding"/>
        </LinearLayout>

        <ListView
            android:id="@+id/widget_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"/>
    </LinearLayout>

</FrameLayout>